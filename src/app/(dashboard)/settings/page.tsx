
import SettingsClient from './SettingsClient';

export const metadata = {
    title: 'ตั้งค่าระบบ | Stock Movement',
    description: 'ตั้งค่าการแจ้งเตือนและระบบอื่นๆ'
};

export default function SettingsPage() {
    return <SettingsClient />;
}
